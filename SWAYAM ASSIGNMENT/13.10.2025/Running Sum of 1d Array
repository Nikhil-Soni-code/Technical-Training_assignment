class Solution {
    public int[] runningSum(int[] nums) {
        int []sum=new int[nums.length];
        int sum2=0;
        for(int i=0;i<nums.length;i++){
            sum2+=nums[i];
            sum[i]=sum2;
        }
        return sum;
        
    }
}
