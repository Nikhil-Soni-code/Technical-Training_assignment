class Solution {
    public int maxArea(int[] height) {
        int i = 0,j = height.length-1;
        int maxStore = 0;
        while(i<j){
            maxStore = Math.max(maxStore,(j-i)*Math.min(height[i],height[j]));
            if(height[i]<height[j])i++;
            else j--;
        }return maxStore;
    }
}
